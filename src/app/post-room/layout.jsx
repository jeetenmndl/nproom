import React from "react";

export const metadata = {
  title: "Sell your Room",
  description: "post room description to be updated",
};

export default function PostRoomLayout({ children }) {
    return (
    <> 
      {children}
    </>
  )
}