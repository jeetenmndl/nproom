import React from "react";

export const metadata = {
  title: "Biartnagar, Near Arniko Campus",
  description: "Room description to be updated",
};

export default function RoomLayout({ children }) {
    return (
    <> 
      {children}
    </>
  )
}